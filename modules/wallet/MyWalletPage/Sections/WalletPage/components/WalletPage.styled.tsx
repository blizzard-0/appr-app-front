import styled from 'styled-components';
import tw from 'twin.macro';

export const StyledWalletListContainer = styled.div`
  ${tw`w-full md:w-small-container-tablet lg:w-small-container mx-auto relative`}
`;
